#include "main.h"

/**
 * _abs - Computes the absolute integer
 *@x: integer I will be computing
 * Return: Always (0) Success
 */

int _abs(int x)
{
	if (x >= 0)
		return (x);
	else
		return (-x);
}
